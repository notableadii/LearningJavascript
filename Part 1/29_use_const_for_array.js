// use const for creating array

const fruits = ['apple', 'banana', 'orange', 'grape', 'mango', 'pineapple'];

fruits.push("strawberry"); // add element to the end of the array


console.log(fruits);

// why should we use const for arrays?
// because we can add elements to the array using push method` 
// we can also remove elements from the array using pop method
// we can also add elements to the beginning of the array using unshift method
// we can also remove elements from the beginning of the array using shift method